# YouTube Automation Pipeline — Dependencies

# Web UI
gradio>=4.0.0

# Video download
yt-dlp>=2024.1.0

# Speech-to-text (GPU accelerated)
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# SMPL body model evaluation (for tracking and Blender rig)
smplx>=0.1.28
trimesh>=4.0.0
# Note: chumpy removed — won't build on Python 3.11. smplx handles
# chumpy-encoded .pkl files with its own backward-compat unpickler.

# PyTorch is installed separately in the Dockerfile as its own cached layer
# (torch==2.3.0+cu121, torchvision==0.18.0+cu121). Do NOT list torch/torchvision
# here — pip would try to reinstall and may pick a CPU-only build.

# Video I/O
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0
scipy>=1.10.0

# LLM interaction (Ollama API)
requests>=2.31.0

# YouTube upload (optional — install when ready)
# google-api-python-client>=2.100.0
# google-auth-oauthlib>=1.1.0
# google-auth-httplib2>=0.1.1

# Utilities
Pillow>=10.0.0
